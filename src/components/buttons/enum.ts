
export enum ButtonStatus {
    primary,
    secondary,
    success,
    danger,
    warning,
    info,
    light,
    dark,
    link,
    disabled,
    float,
}

export enum ButtonType {
    basic,
    outline,
    rounded,
    roundedOutline,
    float,
}

export enum ButtonSize {
    small,
    normal,
    lager,
}